@model List<RealEstateDapperUI.Dtos.PopularLocationDtos.ResultPopularLocationDto>

<div class="locations py-5">
        <div class="container py-lg-5 py-md-4">
            <div class="heading text-center mx-auto">
                <h6 class="title-small">Şehirleri keşfedin</h6>
                <h3 class="title-big">Popüler Yerler</h3>
            </div>
            <div class="row pt-5">
                @foreach(var item in Model)
                {
                <div class="col-lg-3 col-md-4 col-6 mt-md-0 pt-md-0 mt-sm-4 mt-3 pt-md-2">
                    <a href="#url">
                        <div class="box16">
                            <img class="img-fluid" src="@item.ImageUrl" alt="">
                            <div class="box-content">
                                <h3 class="title mb-1">@item.Name</h3>
                                <span class="post">@item.PropertyCount ilan</span>

                            </div>
                        </div>
                    </a>
                </div>
                }
            </div>
        </div>
    </div>